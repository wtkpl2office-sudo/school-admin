# Project: ระบบบริหารจัดการข้อมูลโรงเรียน (โรงเรียนบ้านควนโคกยา)

## เทคโนโลยีหลัก
- **Frontend:** React + TypeScript + Vite + Tailwind CSS
- **Backend:** Supabase (PostgreSQL)
- **Runtime:** Electron (Hybrid Desktop App)
- **Icons:** Lucide React
- **PDF/Printing:** pdf-lib, React-to-Print

## Roadmap & Status

### ✅ ส่วนที่ดำเนินการเสร็จสิ้นแล้ว
*   **งานสารบรรณ (สมบูรณ์):**
    *   ปรับปรุงลายเซ็น (ขยายขนาด + วางเหนือเส้นประ) ทั้งในหนังสือรับและบันทึกข้อความ
    *   ระบบ AI ร่างหนังสือส่งอัตโนมัติ (แยกบรรทัดวันที่ + รูปแบบวันที่ไทยเต็มรูปแบบ)
    *   ปรับปรุงพิกัดวันที่ในบันทึกข้อความให้ตรงมาตรฐาน (10.0cm)
*   **งานบริหารทั่วไป & รายงาน (สมบูรณ์):**
    *   **ระบบรายงานเวลาเรียน:** รองรับสรุปรายวัน, สัปดาห์, เดือน พร้อมคำนวณร้อยละ (%) และสถานะการมาเรียน
    *   **เทมเพลตพิมพ์รายงาน:** จัดรูปแบบราชการไทย ใช้ฟอนต์สารบรรณ ดึงตราสัญลักษณ์โรงเรียนจาก Settings อัตโนมัติ พร้อมส่วนลงชื่อครูเวรและ ผอ.
    *   **ระบบ Export:** รองรับการส่งออกข้อมูลสถิติเป็นไฟล์ Excel
    *   **แก้ไขบั๊ก:** กรองจำนวนนักเรียนตามปีการศึกษาปัจจุบันใน Dashboard และ Analytics และแก้ไขปัญหา Timezone ในการบันทึกวันที่
*   **AI Cowork (ใหม่ - สมบูรณ์ 100%):**
    *   **Virtual Drive:** ระบบจัดการคลังความรู้ครู (00-08) รองรับการอัปโหลดไฟล์และสกัดข้อความอัตโนมัติ
    *   **Chat Hub:** ระบบแชทอัจฉริยะเชื่อมต่อ Gemini API (Retry & Fallback) พร้อมระบบแยก API Key เพื่อความเสถียร
    *   **Context Awareness:** AI อ่านเนื้อหาจริงจากคลังเอกสารมาประมวลผลคำตอบตามสไตล์ครู
*   **ระบบงานสารบรรณ (อัปเดตล่าสุด):**
    *   **PDF Stamping:** ปรับปรุงตำแหน่งตราเกษียณให้อยู่ในหน้าสุดท้าย (หน้าลายเซ็น) และป้องกันการประทับซ้ำซ้อน
    *   **Clean Output:** ตัดคำนำหน้า "สรุป:" "ข้อเสนอ:" เพื่อความสวยงามตามมาตรฐานงานสารบรรณ
*   **ระบบโครงสร้าง:** Auto-Update, Vercel Deployment, Supabase Integration

### 🚧 เป้าหมายที่กำลังดำเนินการ (Current Focus)
*   **LINE Bot Ecosystem (เฟส ๒):** พัฒนาระบบโต้ตอบและดึงข้อมูลอัจฉริยะผ่าน LINE ๑๐๐%
*   **Proactive Notification:** ระบบแจ้งเตือนหนังสือรับใหม่และงานมอบหมายผ่าน LINE อัตโนมัติ
*   **Academic AI Hub:** เพิ่มฟีเจอร์ AI ช่วยวิเคราะห์ผลสัมฤทธิ์ทางการเรียนและช่วยจัดทำ วPA


---

## 🧠 ทักษะการจัดการระบบขั้นสูง (Advanced Engineering Skills & Core Knowledge)

เพื่อให้ตัวเราในอนาคต (หรือ AI ถัดไป) สามารถสืบทอดความรู้และจัดการระบบ Multi-school ได้อย่างราบรื่น:

### 1) การแนบและโหลดฟอนต์ไทยใน Vercel Serverless Function
- **การ Bundle ฟอนต์**: กำหนดค่าใน `vercel.json` โดยใช้ `"functions": { "api/**/*.ts": { "includeFiles": "public/fonts/**" } }` เพื่อบังคับให้ตัวสร้าง Bundle ของ Vercel คัดลอกและจัดเก็บไฟล์ฟอนต์ไทยเข้าไปในรันไทม์ของ Serverless Function
- **การโหลดฟอนต์ที่ปลอดภัย (CORS & Auth Bypass)**: หลีกเลี่ยงการดาวน์โหลดฟอนต์ผ่าน HTTP Fetch ข้ามเน็ตเวิร์กที่อาจติดหน้า Authentication ของ Vercel หรือบล็อก CORS ให้เปลี่ยนมาเรียกอ่านจาก Local Filesystem ของ Vercel ตรงๆ ผ่านพิกัด `path.join(process.cwd(), 'public', 'fonts', 'THSarabunNew.ttf')` หรือ `./dist/fonts/...` แทน โดยมีระบบ `res.ok` ตรวจเช็คเมื่อทำรายการ Backup Fetch

### 2) การป้องกันปัญหา Browser Cache ใน Supabase Storage
- **ปัญหา**: การอัปโหลดไฟล์ที่ประทับตราแล้วทับไฟล์เดิม (upsert: true) บน Supabase Storage จะยังคงได้ URL ตัวเดิม ทำให้เบราว์เซอร์แสดงผลไฟล์ตัวเก่าในแคช
- **การแก้ไข (Cache-Busting)**: ทุกครั้งหลังจากเซฟไฟล์ PDF ที่ประทับตรา ผอ. เสร็จสิ้น ให้ทำการดึง Public URL จาก Supabase และนำมาเสริม query string วันเวลาป้องกันแคช (`?t=timestamp`) จากนั้นนำไปเขียนบันทึกกลับลงในตารางฐานข้อมูล `incoming_docs.file_url` เสมอ เพื่อบังคับให้ผู้ใช้เห็นไฟล์ล่าสุดทันที

### 3) การส่ง LINE Notification เข้ากลุ่มโรงเรียนแบบ Dynamic (Multi-tenant)
- **ปัญหา**: การฮาร์ดโค้ดค่า `LINE_GROUP_ID` ไว้ใน Environment Variables ของ Vercel ทำให้ระบบจำกัดสิทธิ์ใช้ได้โรงเรียนเดียว และสลับเปลี่ยนกลุ่มยาก
- **การแก้ไข**: ออกแบบให้ระบบฝั่ง Webhook วิ่งไปคิวรี่ดึงรหัสกลุ่มโรงเรียน `line_group_id` มาจากตาราง **`settings`** ในฐานข้อมูล Supabase ณ ตอนรันไทม์ เพื่อนำมาใช้เป็นปลายทาง (target) ในการส่งแทน เพื่อความยืดหยุ่นสูงสุดในการสลับกลุ่มหรือใช้งานข้ามโรงเรียนโดยผู้ใช้สลับตั้งค่าได้เองบนหน้าเว็บ

### 4) กลยุทธ์การ Merge Git รักษาโค้ด Local (Merge Strategy Ours)
- **ปัญหา**: โค้ดในเครื่องคอมพิวเตอร์ที่คอมไพล์และบิวต์ผ่านสมบูรณ์แล้ว เกิดการแยกขนาน (Diverged) และเกิด conflict จากประวัติแก้ไขบน GitHub Remote
- **การแก้ไข**: หลีกเลี่ยงการทำ force push ที่ทำลายประวัติ หรือการแก้ conflict ที่เสี่ยงสร้างบั๊กขึ้นใหม่ โดยรันคำสั่ง `git merge origin/multischool -s ours` เพื่อดึงประวัติมาผสานกันโดยยอมรับไฟล์ฝั่ง Local (เครื่องเรา) เป็นหลัก 100% ทันที

### 5) ระบบสร้าง Release และอัปโหลดไฟล์ติดตั้งผ่าน GitHub API
- **ปัญหา**: การอัปโหลดตัวติดตั้งแอปเดสก์ท็อปรวม 3 ไฟล์หลัก (`.exe`, `.blockmap`, `latest.yml`) ขึ้น GitHub Releases ผ่าน CLI มักล้มเหลวเนื่องจากไม่มี Token หรือชื่อไฟล์ไม่ตรงกัน
- **การแก้ไข**: 
  - เพิ่มการตั้งค่า `"artifactName": "${name}-setup-${version}.${ext}"` ใน `package.json` บล็อก `"win"` เพื่อบังคับให้ตัวบิวต์ Electron ผลิตชื่อไฟล์เป็นภาษาอังกฤษเพื่อความเสถียรใน URL ของ GitHub
  - เขียนสคริปต์ Node.js (ใช้ Native Fetch) และ GitHub Token ในการเข้าลบ Assets เก่า และ POST อัปโหลดไฟล์เวอร์ชันล่าสุดทั้ง 3 ไฟล์ขึ้นตรงเข้า Releases API อย่างสมบูรณ์แบบ

### 6) การประสานเวอร์ชันของระบบให้สอดคล้องกัน (App Version Synchronization)
- **ปัญหา**: เมื่อมีการขยับเวอร์ชันของแอปพลิเคชัน (เช่น ใน `package.json`) ตัวเลขเวอร์ชันที่แสดงผลบนหน้าจอโปรแกรมจริง (เช่น Sidebar และหน้าจอตั้งค่าเกี่ยวกับระบบ) กลับไม่ได้รับการอัปเดตตาม และยังค้างแสดงผลเป็นรุ่นเก่าเนื่องจากการฮาร์ดโค้ดหรือการดึงค่าใน `.env` ที่ตกค้างอยู่
- **การแก้ไข**: ทุกครั้งที่มีการขยับรุ่นโปรเจกต์ (Bump Version) จะต้องเข้าไปตรวจสอบและอัปเดตเลขรุ่นให้ตรงกันใน 3 ส่วนหลักเสมอ:
  1. ไฟล์ `package.json` ฟิลด์ `"version"`
  2. ไฟล์ `.env` คีย์ `VITE_APP_VERSION`
  3. ค่าเริ่มต้นสำรอง (Fallback Value) ในโค้ดแสดงผล ได้แก่ `src/App.tsx` (บรรทัดแสดงผล Version ของ Sidebar) และ `src/pages/Settings.tsx` (บรรทัดแสดงผลเวอร์ชันปัจจุบันในเมนูเกี่ยวกับระบบ) เพื่อป้องกันหน้าจอแสดงเลขรุ่นสับสนไม่ตรงกับประวัติประวัติการปรับปรุง (Changelog) ล่าสุด

### 7) การแก้ปัญหา Vercel Function Timeout + ข้อควรระวัง Multi-School
- **สาเหตุ**: `vercel.json` ไม่มี `maxDuration` → Vercel ใช้ default timeout ต่ำ (~10-15 วินาที) ฟังก์ชันหนัก เช่น `telegram-webhook`, `line-webhook`, `ocr-process` จะถูก kill กลางคัน
- **การแก้ไขที่ถูกต้อง**: ใช้ **wildcard pattern เดียว** เพิ่ม `maxDuration` เข้าไปใน `api/**/*.ts` ที่มีอยู่แล้ว:
  ```json
  "functions": {
    "api/**/*.ts": {
      "includeFiles": "public/fonts/**",
      "maxDuration": 60
    }
  }
  ```
- **⚠️ ข้อควรระวัง Multi-School**: ห้ามระบุ specific file pattern เช่น `"api/telegram-webhook.ts": { "maxDuration": 60 }` เพราะแต่ละโรงเรียนอาจมีไฟล์ใน `api/` ไม่เท่ากัน หรือไม่ได้เปิดใช้ครบทุกระบบ → Vercel จะ deploy fail ทันทีด้วย error: `"The pattern doesn't match any Serverless Functions inside the api directory"`
- **Vercel Hobby Plan Limits (ตรวจสอบแล้ว ส.ค. 2569)**:
  - `maxDuration` สูงสุด: **300 วินาที** (5 นาที)
  - Cron jobs: สูงสุด 100 job/project
  - Cron **frequency**: รันได้ **วันละ 1 ครั้งเท่านั้น** — หากตั้งถี่กว่านี้ (เช่น ทุกชั่วโมง) deployment จะล้มเหลวทันที

### 8) Git Remote & Branch Structure ของระบบ Multi-School
- Remote `origin` → `officebkky-sketch/school-admin` (โรงเรียน 1)
- Remote `school2` → `wtkpl2office-sudo/school-admin` (โรงเรียน 2)
- **Branch หลักที่ Vercel ทั้ง 2 โรงเรียน track**: `multischool` (ไม่ใช่ `main`)
- การส่งโค้ดขึ้นระบบ:
  ```bash
  git push origin multischool   # โรงเรียน 1
  git push school2 multischool  # โรงเรียน 2
  ```
- หาก push ไปยัง `school2` ติดปัญหา Permission Denied ให้ใช้วิธี **Sync Fork** จากหน้า GitHub Web UI แทน

### 9) กฎการแสดงผลวันที่ DD-MM-YYYY ทั่วทั้งระบบ (Date Formatting Invariant)
- **ข้อกำหนด**: ทุกข้อความแจ้งเตือนทาง Telegram, LINE Flex และตารางแสดงผลบนหน้าเว็บ (เช่น `doc_date`, `sender_doc_date`, `action_deadline`) ต้องแปลงเป็นรูปแบบ วัน-เดือน-ปี พ.ศ. เช่น `27-08-2569` (DD-MM-YYYY) โดยใช้ฟังก์ชัน `formatDateDMY()` ใน `src/lib/dateUtils.ts` หรือ `toDMYString()`
- **ข้อยกเว้น**: การผูกค่าภายในของ HTML `<input type="date">` ให้ใช้ `YYYY-MM-DD` ตามมาตรฐานของ Browser เท่านั้น

### 10) การสแกนหนังสือขั้นตอนเดียวและป้องกัน Notification Spam (Silent Background OCR)
- **ข้อกำหนด**: โมดอลลงรับหนังสือให้ AI สแกน Auto-fill ทันทีที่เลือกไฟล์หนังสือนำ (Single-step flow)
- **การแจ้งเตือน**: เมื่อกดบันทึกและเสนอ ให้ส่งแจ้งเตือนรอบเดียวแบบ Unified Rich Card (มีเลขรับ, เรื่อง, สาระสำคัญ `<blockquote>`, กำหนดการ, ป้ายความเร่งด่วน 🔴 🟠 🟡 🟢, แนะนำครูผู้รับงาน, และไฟล์แนบครบทุกไฟล์)
- **Anti-Spam**: การเรียก API พื้นหลัง `/api/ocr-process` เพื่อสกัดความจำ RAG ต้องระบุ `{ silent: true }` เสมอ เพื่อไม่ให้ส่งข้อความ Telegram ซ้ำสอง

### 11) ระบบคิวหนังสือรอเกษียณต่อเนื่อง (Auto-Next Document Queue / Inbox Zero)
- **ข้อกำหนด**: เมื่อ ผอ. เกษียณสั่งการหนังสือฉบับหนึ่งสำเร็จใน Telegram (`executeDocAssignment`) ระบบต้องคิวรี่หาหนังสือรอเกษียณฉบับถัดไปในคิวทันที (`status in ['pending', 'waiting_proposal']`) แล้วส่งการ์ดสรุปพร้อมปุ่มสั่งการต่อท้ายทันที เพื่อให้ ผอ. สั่งการต่อเนื่องได้โดยไม่ต้องเลื่อนแชท
- **Inbox Zero**: เมื่อเกษียณครบทุกฉบับแล้ว ให้ส่งข้อความแจ้งเตือน Inbox Zero (`🎉 ยอดเยี่ยมมากค่ะ ผอ.! ขณะนี้เกษียณสั่งการหนังสือครบทุกฉบับในคิวเรียบร้อยแล้วค่ะ 🌸✨`)

### 12) การเสนอหนังสือแบบกลุ่ม (Bulk Propose Completeness)
- **ข้อกำหนด**: ฟังก์ชัน `handleBulkPropose` ต้อง parse ข้อมูลจาก `remark` (ได้แก่ `proposal_summary`, `sender_doc_number`, `sender_doc_date`) และแสดงป้ายความเร่งด่วน 🔴 🟠 🟡 🟢 พร้อมกำหนดการ `DD-MM-YYYY` และลิงก์ไฟล์แนบทุกฉบับครบถ้วน

### 13) Vercel Serverless Function Universal Dual-Runtime Pattern (ป้องกัน 504 Gateway Timeout)
- **ปัญหา**: เมื่อ handler ใน `api/*.ts` ประกาศเป็น `export default async function handler(req: Request): Promise<Response>` แล้วคืนค่า `new Response(...)` บน Vercel Node.js Serverless Function (ซึ่งไม่ได้เปิด Edge Runtime) ระบบจะส่ง arguments เข้ามาเป็น `(req: any, res?: any)` ของ Node.js HTTP Server หากไม่เรียก `res.status().json()` หรือ `res.end()` ตัว Node.js server จะไม่เคยส่งสัญญาณปิดการเชื่อมต่อ ทำให้เกิด **HTTP 504 Gateway Timeout**
- **การแก้ไข (Universal Dual-Runtime)**: ต้องเขียน handler ให้รองรับทั้งสองสภาพแวดล้อม:
  - ตรวจสอบว่ามี `res?.status` หรือไม่ ถ้ามีให้ตอบผ่าน `res.setHeader(...)` และ `res.status(status).json(data)`
  - ถ้าไม่มี `res` (เช่น Edge Runtime หรือ Fetch mock) ให้ตอบผ่าน `new Response(JSON.stringify(data))`
  - จัดการ CORS Preflight (OPTIONS 204) และ Universal Request Body Parsing (`req.body` vs `req.json()`) ทุกครั้ง

### 14) DB-First & Instant Optimistic UI State Transition
- **ปัญหา**: ในขั้นตอนเสนอหนังสือ (เดี่ยวหรือชุด) หากรอการส่งแจ้งเตือนภายนอก (`sendBulkFlexCarousel`, `sendTelegramNotification`) ให้เสร็จก่อนแล้วจึงบันทึกฐานข้อมูล เมื่อ LINE/Telegram ช้า 5-15 วิ หน้าจอจะหมุนค้างและสถานะในตารางไม่ยอมเปลี่ยน
- **การแก้ไข**: 
  1. **DB-First**: สั่ง `await supabase.from('incoming_docs').update({ status: 'pending' })` ทันที (< 200ms)
  2. **Instant Optimistic UI**: อัปเดต State บนหน้าจอทันที (`setDocs(...)`, `setSelectedHoldingIds([])`) ให้ป้ายสถานะเปลี่ยนเป็น "รอ ผอ. เกษียณ" ในเสี้ยววินาที และปิด Modal / เคลียร์ Checkbox ทันที
  3. **Concurrent Background Dispatch**: รันการส่งแจ้งเตือนทั้ง LINE และ Telegram แบบคู่ขนาน (`Promise.allSettled`) ในพื้นหลัง โดยไม่บล็อกการใช้งานของผู้ใช้

### 15) Google Apps Script & Drive Archiving Resilience Standard
- **การจัดระเบียบปี พ.ศ. ตามวันที่ลงรับหนังสือ (Strict Receiving Date Standard)**:
  - การแยกโฟลเดอร์ตามปี พ.ศ. (`ปี 2569`) **ต้องอ้างอิงจาก "วันที่ลงรับหนังสือ" (`doc_date`) เสมอ 100%** ห้ามใช้ Regex เดาปีจากข้อความในชื่อเรื่อง (`subject`) เด็ดขาด เพื่อป้องกันหนังสือที่มีชื่อโครงการปีเก่าหลุดไปอยู่ผิดโฟลเดอร์ปี
  - สำหรับไฟล์เดิมที่มีอยู่ใน Google Drive ให้ใช้ `file.getDateCreated()` (วันที่ไฟล์ถูกบันทึก/อัปโหลดเข้าระบบ) เป็นเกณฑ์ปี พ.ศ.
- **การจำแนกหนังสือนำเข้าโฟลเดอร์หนังสือรับ**:
  - หนังสือนำหลักที่มีรูปแบบ `${doc_number}_เรื่อง_${subject}.pdf`, ขึ้นต้นด้วยเลขที่รับ (`/^[0-9]+_/`), หรือขึ้นต้นด้วย `ว`, `ศธ`, `ที่` ต้องจัดเข้าโฟลเดอร์ **`01_หนังสือรับ`** เสมอ ไม่ใช่ตกไปที่ `07_เอกสารทั่วไป`
  - ฟังก์ชันจัดระเบียบไฟล์เก่า (`organizeExistingFiles`) ต้องสแกนโฟลเดอร์ `07_เอกสารทั่วไป` ด้วยเสมอ เพื่อดึงหนังสือนำที่เคยหลงเข้าไปกลับมาจัดหมวดหมู่ให้ถูกต้อง
- **Scoped Search & 4-Minute Time Guard (ป้องกัน GAS 6-Minute Timeout)**:
  - ห้ามกวาดค้นหาจาก `DriveApp.getRootFolder()` ทั้งไดรฟ์เด็ดขาด ให้ค้นหาเฉพาะโฟลเดอร์ของระบบสารบรรณ (`SchoolAdminDocs`, `incoming`, `outgoing`, ฯลฯ)
  - ต้องมี Time Guard ปลอดภัยตัดรอบที่ 4 นาที (`MAX_RUNTIME_MS = 240000`) ป้องกันการโดน Google ตัดจบด้วย `Exceeded maximum execution time` และรองรับการกดรันต่อจนกว่าจะครบ 100%
- **Apps Script Syntax & Code Presentation Standard**:
  - หลีกเลี่ยง Block Comment `/** ... */` ให้ใช้ `//` เพื่อป้องกันปัญหา `Unexpected token '*'` จากการคัดลอกตกหล่น
  - String ข้อความในโค้ดต้องสั้น กระชับ บรรทัดเดียว เพื่อป้องกันปัญหาโปรแกรมแชทตัดบรรทัด (Line-wrapping) กลางเครื่องหมายคำพูดจนเกิด `SyntaxError: Invalid or unexpected token`

---

### 📅 บันทึกแผนงานล่าสุด
- **๑๑ มิถุนายน ๒๕๖๙ (v1.1.6 Hotfix):**
    - **ปลดล็อกขีดจำกัดไฟล์แนบทาง LINE:**
        - แก้ไขขีดจำกัดจำนวนปุ่มไฟล์แนบใน LINE Flex Message สำหรับครูผู้รับงาน จากเดิมจำกัด 2 ปุ่ม เป็นสูงสุด 10 ปุ่ม (ส่งไฟล์แนบเพิ่มเติมได้ถึง 8 ปุ่ม)
        - อัปเดตพร้อมกันทั้งฝั่งแอปเดสก์ท็อป ([IncomingDocs.tsx](file:///C:/Users/Phairot%20M/Desktop/school-admin/src/pages/IncomingDocs.tsx)) และบอทหลังบ้าน Vercel ([line-webhook.ts](file:///C:/Users/Phairot%20M/Desktop/school-admin/api/line-webhook.ts))
    - **แก้ไขบั๊กตราประทับ ผอ. ผ่าน LINE:**
        - แก้ไขบอทหลังบ้าน ([line-webhook.ts](file:///C:/Users/Phairot%20M/Desktop/school-admin/api/line-webhook.ts)) ให้ดึงชื่อโรงเรียน (`school_name`) มาต่อท้ายตำแหน่ง ผอ. บนตราประทับ PDF อัตโนมัติ (เช่น "ผู้อำนวยการโรงเรียนบ้านควนโคกยา") ป้องกันการแสดงตำแหน่งว่างเปล่า และทำให้ข้อมูลตราประทับถูกต้องตรงกับระบบหน้าจอหลัก
- **๙ มิถุนายน ๒๕๖๙ (ปัจจุบัน - v1.1.6 Release):**
    - **ระบบตราประทับ ผอ. แบบ Dynamic (Dynamic School Name on Stamping):**
        - ปรับปรุงให้ดึงชื่อโรงเรียน (`school_name`) จากตารางตั้งค่าระบบมาแสดงผลร่วมกับตำแหน่ง ผอ. บนตราประทับ PDF อัตโนมัติ (เช่น "ผู้อำนวยการโรงเรียนบ้านควนโคกยา" หรือชื่อโรงเรียนใหม่อื่นๆ ตามที่สลับ Instance) ช่วยให้ระบบมีความยืดหยุ่นและแยกข้อมูลข้ามโรงเรียนได้อย่างสมบูรณ์แบบ 100%
        - แก้ไขไฟล์ [IncomingDocs.tsx](file:///C:/Users/bkky9/OneDrive/Desktop/school-admin-multischool/src/pages/IncomingDocs.tsx) สำเร็จลุล่วงและอัปโหลดขึ้น GitHub Releases
- **๒๙ พฤษภาคม ๒๕๖๙ (v1.1.4 Release):**
    - **ระบบแจ้งเตือนและแชทบอท LINE (Flex Carousel for Proactive Actions):**
        - **Flex Message Carousel (งานรอสั่งการ):** พัฒนาระบบส่งกลุ่ม "งานรอสั่งการ (Waiting Proposal)" ไปยังกลุ่ม LINE ของ ผอ. และการโต้ตอบของคำสั่งบอท LINE โดยตอบกลับด้วยรูปแบบ Flex Message Carousel (การ์ดสไลด์แบบเลื่อน) ดึงรายการหนังสือรับสถานะ `pending` แบบ Dynamic พร้อมปุ่มลิงก์สั่งการ
        - **Backend Integration:** อัปเดตฟังก์ชัน `lineNotify.ts` และ Webhook Server (`api/line-webhook.ts`) เพื่อเชื่อมต่อข้อมูลและประมวลผลคำสั่งอย่างสมบูรณ์
    - **การจัดทำ Release & Deploy (Desktop Client v1.1.4):**
        - **GitHub Releases Deployment:** คอมไพล์และบิวด์ระบบเดสก์ท็อปผ่าน `electron-builder` ร่วมกับ GitHub Token อัปโหลดตัวติดตั้งหลัก `school-admin-v2-setup-1.1.4.exe` และ `.blockmap` ขึ้นสู่ระบบ GitHub Releases ภายใต้เวอร์ชัน 1.1.4 สำเร็จเสร็จสิ้น
- **๒๘ พฤษภาคม ๒๕๖๙:**
    - **ระบบบริหารจัดการสิทธิ์ผู้ใช้งาน (Access Control Refactoring) [ความปลอดภัยและความเป็นส่วนตัว]:**
        - **Granular Permissions Matrix:** ปรับโครงสร้างสิทธิ์การเข้าถึงเมนูต่างๆ ของบัญชีระดับ "ครู (Teacher)" ให้จำกัดเฉพาะเมนูสิทธิ์พื้นฐาน และย้ายเมนูงานวิชาการและงานงบประมาณไปควบคุมด้วยสิทธิ์พิเศษทั้งหมด
        - **New Permission Keys:** ติดตั้งสิทธิ์พิเศษใหม่ 2 รายการ ได้แก่ สิทธิ์งานวิชาการ (`access_academic`) และสิทธิ์งานงบประมาณ (`access_finance`) ลงในหน้าจัดการสิทธิ์ ([Users.tsx](file:///C:/Users/bkky9/OneDrive/Desktop/school-admin-multischool/src/pages/Users.tsx))
        - **Double-Gate Security Verification:** อัปเดตเงื่อนไข Sidebar Menu ใน [App.tsx](file:///C:/Users/bkky9/OneDrive/Desktop/school-admin-multischool/src/App.tsx) เพื่อตรวจสอบสิทธิ์รายหมวดหมู่ และเพิ่มระบบดีดกลับหน้าแดชบอร์ดอัตโนมัติใน `useEffect` เมื่อตรวจพบการเข้าใช้งานหน้าควบคุมโดยไม่ได้รับสิทธิ์
        - **Access Audit Document:** จัดทำคู่มือรายงานและวิเคราะห์ช่องโหว่ความปลอดภัยระดับสิทธิ์การเข้าถึงและ RLS ฐานข้อมูล บันทึกไว้ในไฟล์ [ACCESS_CONTROL_AUDIT.md](file:///C:/Users/bkky9/OneDrive/Desktop/school-admin-multischool/ACCESS_CONTROL_AUDIT.md)
        - **LINE Bot Feature Roadmap:** บันทึกแผนพัฒนาปรับปรุงการตอบกลับของแชทบอท โดยเตรียมอัปเกรดคำสั่ง "งานรอสั่งการ" และ "งานรอเกษียณ" ให้ตอบกลับเป็น Flex Message Carousel (การ์ดสไลด์แบบเลื่อน) ดึงข้อมูลตาราง `incoming_docs` (สถานะ `pending`) พร้อมปุ่มกดสั่งการ เพื่ออำนวยความสะดวกในการกดเกษียณงานผ่าน LINE ทันทีโดยมีความปลอดภัยสูงและไม่มีความเสี่ยงกระทบระบบหลัก
- **๒๑ พฤษภาคม ๒๕๖๙:**
    - **ระบบ LINE OA Connect [ความสำเร็จครั้งใหญ่]:**
        - **Webhook Server:** ติดตั้งระบบ Webhook ในรูปแบบ Vercel Serverless Function (`api/line-webhook.ts`) รองรับการสื่อสาร ๒ ทาง (Two-way communication)
        - **Email Authentication:** พัฒนาระบบยืนยันตัวตนอัจฉริยะผ่านอีเมลใน LINE (Case-insensitive) พร้อมผูกบัญชีเข้ากับฐานข้อมูลโรงเรียนอัตโนมัติ
        - **Intelligent RAG (LINE):** ยกโครงสร้าง "คลังปัญญาโรงเรียน" (Hybrid Search) จากแอปคอมพิวเตอร์มาใส่ใน LINE ทำให้บอทสามารถอ่านไฟล์ระเบียบและโครงการในฐานข้อมูลมาตอบครูได้ทุกที่ทุกเวลา
        - **Real-time DB Stats:** เชื่อมต่อบอท LINE เข้ากับสถิตินักเรียนและครูโดยตรง รองรับการนับเพศแบบยืดหยุ่น (ช/ญ) และสรุปแยกตามศาสนา
        - **Fix 405/404/429:** แก้ปัญหาทางเทคนิคบน Vercel ทั้งเรื่องการจัดเส้นทาง (Routing), ชื่อรุ่น AI (Gemini 2.0/1.5) และการบริหารจัดการโควตา API
    - **AI Cowork Upgrade (Smart Action Hub):**
        - ปรับปรุงหน้าจอ AI Cowork ใหม่ เพิ่ม **"แผงเครื่องมืออัจฉริยะ" (Quick Action Cards)** ๖ ด้าน: ออกแบบใบงาน, ร่างบันทึกข้อความ, PR โรงเรียน, กิจกรรมเชิงสร้างสรรค์, วิเคราะห์ข้อมูล และแผนการสอน
        - ติดตั้ง **Specialized AI Personas:** ปรับจูน Prompt ให้ AI สวมบทบาทผู้เชี่ยวชาญตามงานที่ได้รับ (เช่น ครูวิชาการ, นักประชาสัมพันธ์, นักวิเคราะห์)
    - **ระบบความเสถียรและฐานข้อมูล:**
        - **Storage Sanitization:** แก้ปัญหา "Invalid key" ในการบันทึกหนังสือรับ โดยเปลี่ยนระบบการตั้งชื่อไฟล์จากภาษาไทยเป็นรหัสตัวเลข (Timestamp) อัตโนมัติ
        - **RLS Update:** ปรับสิทธิ์การเข้าถึงข้อมูล (students table) ให้บอท LINE (Anon Key) สามารถดึงสถิติพื้นฐานมาตอบครูได้อย่างปลอดภัย
        - **TypeScript Build Fix:** เคลียร์ Error ทั้งหมดในระบบ Build ทำให้การ Deploy บน Vercel ราบรื่น ๑๐๐%
- **๑๘ พฤษภาคม ๒๕๖๙:**
    - **ระบบรายงานสถิตินักเรียน (LEC) [ปรับปรุงความสมบูรณ์]:**
        - ปรับตำแหน่งส่วนลงชื่อ (ตำแหน่ง ครู, วันที่, เบอร์โทรศัพท์, และผู้อำนวยการสถานศึกษา) ให้ขยับไปทางซ้าย ๙ สเปซบาร์ เพื่อความสวยงามและสมดุลตามความต้องการของผู้ใช้
        - แก้ไขฟังก์ชันการแสดงผลตัวเลข (toThaiDigits) ให้แสดงเลข "๐" (ศูนย์ไทย) ในกรณีที่ไม่มีจำนวนนักเรียนในระดับชั้นนั้นๆ แทนการเว้นว่างไว้ เพื่อความถูกต้องตามมาตรฐานงานสารบรรณ
        - ปรับปรุงการจัดวางตราสัญลักษณ์โรงเรียนในรายงาน LEC-1 และ LEC-2 โดยดึงข้อมูลโดยตรงจาก "ตั้งค่าระบบ" (Settings) ปรับขนาดเป็น ๘๐px และขยับตำแหน่งขึ้นด้านบน -๒๐px เพื่อความสวยงามและเป็นสากล
        - แก้ไขรายงาน "พิมพ์รายชื่อนักเรียน (กำหนดเอง)" ให้ดึงตราสัญลักษณ์โรงเรียนจากหน้าตั้งค่าระบบมาแสดงผลแทนโลโก้เดิม เพื่อให้เอกสารทุกส่วนในระบบเป็นมาตรฐานเดียวกัน
        - พัฒนาระบบจัดการขนาดตารางอัตโนมัติ (Auto-Balancing) ในหน้าพิมพ์รายชื่อนักเรียน โดยกำหนดความกว้างคอลัมน์พื้นฐานให้คงที่และอนุญาตให้คอลัมน์ชื่อ-สกุล/คอลัมน์เปล่าขยายพื้นที่ได้เต็มประสิทธิภาพ เพื่อแก้ปัญหาตารางแคบและเพิ่มความสวยงามในการพิมพ์
    - **ระบบเบิกค่าสาธารณูปโภค (Utilities) [ใหม่ & อัปเกรด]:**
        - ติดตั้งโมดูลบริหารจัดการค่าสาธารณูปโภค รองรับการเก็บข้อมูล "จำนวนหน่วยที่ใช้" (Units Used) และ "เล่มที่/เลขที่" ตามรูปแบบใบเสร็จจริง
        - พัฒนาระบบรองรับ "หลายใบแจ้งหนี้ต่อการเบิกจ่าย ๑ ครั้ง" พร้อมระบบคำนวณยอดรวมและหน่วยรวมอัตโนมัติ
        - พัฒนาระบบ **Auto-Generator สำหรับบันทึกข้อความและใบปะหน้า** (หน้า ๑ และ หน้า ๒) ตามระเบียบงานสารบรรณ บังคับพิมพ์แนวตั้งอัตโนมัติ พร้อมตราครุฑ
        - เพิ่มระบบ **Smart Default** ดึงชื่อหัวหน้าฝ่ายงบประมาณมาเป็นผู้เบิกให้อัตโนมัติ โดยเชื่อมโยงกับฐานข้อมูลครูที่ตั้งค่าตำแหน่งไว้
        - ติดตั้งระบบ **"ร่างเนื้อหาด้วย AI"** และระบบจัดการสถานะ (รอดำเนินการ -> อนุมัติแล้ว) เพื่อความสะดวกในการติดตามงานการเงิน
    - **งานสารบรรณ (หนังสือรับ):**
        - ปรับปรุงระบบประทับตราดิจิทัล (Digital Stamping) ให้รองรับเอกสารหลายหน้า โดยแยกส่วนการทำงาน: **เลขรับ (หน้า ๑ เสมอ)** และ **ข้อความเกษียณเสนอ/คำสั่ง ผอ. (เลือกหน้าได้เอง)** เพื่อป้องกันการทับซ้อนเนื้อหาสำคัญ
        - แก้ไขปัญหาการประทับตราซ้ำ (Double Stamping) ในขั้นตอนการเกษียณของ ผอ. โดยระบบจะประทับเฉพาะตราคำสั่งใหม่เพียงอย่างเดียว
        - เปลี่ยนรูปแบบการตั้งชื่อไฟล์หลังเกษียณหนังสือให้เป็นระเบียบและสื่อความหมาย: **`[เลขรับ]_เรื่อง_[ชื่อเรื่อง].pdf`** แทนรูปแบบเดิมที่เป็นเลขสุ่ม
    - **งานสารบรรณ (บันทึกข้อความ):**
        - อัปเกรดระบบ **"ร่างด้วย AI"** ในหน้าออกเลขบันทึกข้อความ โดยเพิ่มช่อง **"รายละเอียดใจความสำคัญ"** เพื่อให้ AI ร่างเนื้อหาได้ตรงประเด็นและมีความละเอียดแม่นยำสูงขึ้น (เลียนแบบระบบหนังสือส่ง)
    - **งานบุคคล (จัดการข้อมูลครู):**
        - ปรับปรุงโครงสร้างข้อมูลครูให้รองรับ "กลุ่มสาระการเรียนรู้" แยกกับ "ฝ่าย/งานที่รับผิดชอบ" เพื่อรองรับภาระงานที่หลากหลายของคุณครู (หมวก ๒ ใบ)
        - เพิ่มระบบ **Quick Suggestions** สำหรับ ๘ กลุ่มสาระการเรียนรู้ และตำแหน่งหัวหน้าฝ่ายต่างๆ เพื่อความรวดเร็วและเป็นมาตรฐานในการกรอกข้อมูล
    - **หน้าข้อมูล student (Students):**
        - ปรับปรุงระบบให้เลือกปีการศึกษาล่าสุดจากฐานข้อมูลเป็นค่าเริ่มต้นโดยอัตโนมัติ (Dynamic Default Year) แทนการใช้ค่าคงที่ เพื่อให้แสดงข้อมูลนักเรียนที่เป็นปัจจุบันที่สุดทันทีเมื่อเปิดหน้า
        - ปรับปรุงไฟล์แม่แบบ (Template CSV) และระบบนำเข้าข้อมูลนักเรียน (importStudentData) ให้ตรงตามรูปแบบไฟล์จริงจากระบบ DMC ๑๐๐% โดยเพิ่มคอลัมน์ "คำนำหน้าชื่อ" ของผู้ปกครอง บิดา และมารดา เพื่อป้องกันปัญหาลำดับข้อมูลเลื่อน (Column Shift) และรองรับการนำเข้าข้อมูลที่มีความละเอียดสูงขึ้น
    - **การเตรียมความพร้อมสู่ระบบ Multi-School (ขยายผล):**
        - วางโครงสร้างโค้ดชุดเดียว (Single Source of Truth) รองรับการแยก Instance ผ่าน Supabase และ Vercel
        - ระบุจุดเชื่อมต่อ Google Drive (`GAS_URL`) ใน `src/lib/storage.ts` เพื่อเตรียมการย้ายไปเก็บในระบบตั้งค่า (Settings) เพื่อความสะดวกในการเปลี่ยน Drive ของแต่ละโรงเรียนโดยไม่ต้องแก้ไขโค้ด
    - **ระบบ "สมองอัจฉริยะ" (School Intelligence Hub - RAG System) [เฟส ๑ สำเร็จ ๑๐๐%]:** 
        - **สำเร็จในวันนี้ (๒๐ พ.ค. ๖๙):**
            - **Security Layer:** ติดตั้งระบบสิทธิ์ RLS ขั้นสูง แยกคลังสมองส่วนกลาง (เฉพาะแอดมินแก้ไข) และห้องส่วนตัวครู (ครูแต่ละคนเห็นเฉพาะของตนเอง) อย่างเด็ดขาด
            - **Teacher Private Workspace (Virtual Drive):** สร้างพื้นที่ทำงานส่วนตัวให้ครูแต่ละคนอัปโหลดไฟล์ และเรียกใช้ AI ส่วนตัว (Sandbox Mode) เพื่อวิเคราะห์เอกสารส่วนบุคคลได้โดยไม่ปะปนกับคลังส่วนกลาง
            - **Storage Integration:** แก้ปัญหา Storage Bucket โดยการเชื่อมต่อระบบ Virtual Drive เข้ากับ Google Drive ของโรงเรียนผ่าน GAS ทำให้การเก็บไฟล์มีประสิทธิภาพและประหยัดพื้นที่ Supabase
            - **Hybrid Search Algorithm:** พัฒนาระบบค้นหาแบบผสมผสาน (Vector + Keyword Search) เพื่อแก้ปัญหาการค้นหาไม่พบข้อมูลเมื่อโควตา Gemini API เต็ม ทำให้ระบบยังคงดึงข้อมูลมาตอบได้แม่นยำเสมอ
        - **ก้าวต่อไป:** เริ่มงานเฟส ๒ พัฒนาโมดูลบริหารงาน ๔ ฝ่าย (วิชาการ, พัสดุ, ทั่วไป, กิจการนักเรียน) แบบบูรณาการ
    - **๑๘ พฤษภาคม ๒๕๖๙:**

        - เพิ่มระบบบันทึกการตั้งค่าจำนวนเงิน (ค่าเครื่องแบบ/อุปกรณ์) ลงใน Local Storage เพื่อให้ข้อมูลคงอยู่แม้รีเฟรชหน้าเว็บ
        - ปรับปรุงการแสดงผลระดับชั้นในตารางให้เป็นเลขไทยทั้งหมด (เช่น อ.๑/๑, ป.๒/๑) ตามมาตรฐานงานสารบรรณ
        - ขยายและเพิ่มคอลัมน์ "เบอร์โทรศัพท์" ในแบบหลักฐานการจ่ายเงิน และดึงข้อมูลอัตโนมัติจากฐานข้อมูล
        - จัดรูปแบบตารางใหม่: ย้ายแถว "รวมเป็นเงินทั้งสิ้น" ไว้บรรทัดล่างสุด และปรับจำนวนแถวให้ตรงตามจำนวนนักเรียนจริง (ไม่มีแถวว่างเกิน)
        - ปรับปรุง On-screen Preview: แสดงผลเลียนแบบกระดาษ A4 จริงบนหน้าจอ พร้อมใช้ฟอนต์ TH Sarabun New และจัดหัวตาราง "วัน/เดือน/ปี ที่รับเงิน" ให้ถูกต้อง
        - ระบบจดจำข้อมูลรายชั้น: เพิ่มฟังก์ชันจดจำชื่อผู้จ่ายเงินแยกตามระดับชั้นเรียน (Auto-fill เมื่อเปลี่ยนชั้น) และบันทึกถาวรลง Local Storage
        - **ระบบแยกชุดเอกสาร (Dynamic Layout):** เพิ่มฟังก์ชันเลือกพิมพ์แยกประเภท "ค่าเครื่องแบบ" หรือ "อุปกรณ์การเรียน" โดยระบบจะปรับหัวข้อ, คอลัมน์ตาราง, และการคำนวณยอดรวม (Baht Text) ให้ถูกต้องตามประเภทที่เลือกโดยอัตโนมัติทั้งในหน้าจอและใบพิมพ์
        - แก้ไขคำผิด: ลบคำว่า "เงิน" ที่ซ้ำซ้อนในหัวข้อ "แบบหลักฐานการจ่ายเงินอุดหนุน..." เพื่อความถูกต้องตามหลักภาษาไทย
        - ปรับปรุงการพิมพ์: ขยายพื้นที่ตารางให้เต็มหน้ากระดาษ A4 (Margin 3mm) และจัดตำแหน่งลายเซ็น ผอ./ผู้จ่ายเงิน ให้สมดุลสวยงาม

        - แก้ไขปัญหา Filter: ปรับปรุงระบบ Fetching ให้มีความเสถียรสูง แก้ไขบั๊กเลือกชั้นเรียนแล้วรายชื่อไม่ขึ้น
    - **ระบบรายงานสถิตินักเรียน (LEC) [ปรับปรุงรูปแบบใหม่]:**
        - เปลี่ยนฟอนต์เอกสารทั้งหมดเป็น TH Sarabun New เพื่อความสุภาพและเป็นทางการตามมาตรฐานราชการ
        - ปรับปรุงส่วนลงชื่อใน LEC-1 และ LEC-2 โดยใช้ระบบ Table Alignment ทำให้ชื่อในวงเล็บอยู่กึ่งกลางใต้เส้นลงชื่อพอดี 100%
        - บังคับใช้เลขไทย (Thai Digits) สำหรับข้อมูลตัวเลขทั้งหมดในเอกสาร (วันที่, ลำดับ, เลขประจำตัว, ระดับชั้น)
        - ปรับลดระยะห่างระหว่างบรรทัด (Line-height) ให้กระชับสวยงาม และป้องกันการตัดบรรทัดของข้อความส่วนลงชื่อ
- **๑๔ พฤษภาคม ๒๕๖๙:**
    - **ระบบจ่ายเงินเรียนฟรี (๑๕ ปี) [สมบูรณ์]:**
        - ปรับปรุงการจัดวางส่วนลงชื่อ (Signature Blocks) ให้สมดุล 100% โดยใช้ระบบ Column Balancing ให้ชื่อในวงเล็บอยู่กึ่งกลางเส้นบรรทัดแม่นยำ
        - เพิ่มระบบคำนวณเงินอัตโนมัติแยกตามระดับชั้น (ปฐมวัย/ประถมศึกษา) พร้อมแปลงยอดรวมเป็นตัวอักษรภาษาไทย (Baht Text) ที่ถูกต้องสละสลวย
        - เชื่อมโยงข้อมูลผู้อำนวยการโรงเรียนและชื่อโรงเรียนจากระบบการตั้งค่า (Settings) อัตโนมัติ
    - **งานสารบรรณ (หนังสือส่ง) [อัปเกรด AI Draft]:**
        - พัฒนาระบบ "ร่างด้วย AI" ให้มีความยืดหยุ่นสูง: รองรับทั้งการร่างตอบกลับ (อิงหนังสือรับ) และการร่างเรื่องใหม่โดยตรง (ไม่อิงหนังสือรับ)
        - ปรับปรุง Validation: อนุญาตให้ร่างตอบกลับได้ทันทีโดยไม่ต้องระบุรายละเอียดเพิ่ม (AI วิเคราะห์จากหัวข้อต้นเรื่อง)
        - แก้ไขปัญหาคำลงท้ายซ้ำซ้อน: เพิ่มตัวเลือก "ไม่ระบุ / ใช้ตามเนื้อหา AI" และปรับ Prompt ให้ AI เน้นเฉพาะเนื้อหาใจความสำคัญ
        - อัปเกรดความเสถียร: เพิ่มระบบ Fallback วนลูปโมเดล (Gemini 2.0/1.5 Flash) และรองรับหลาย API Version (v1/v1beta)
    - **AI Cowork [ปรับปรุงความเสถียร]:**
        - แก้ไขปัญหา Chat Hub ไม่ตอบสนอง (AI ไม่คืนค่าตอบตอบ) โดยการนำระบบ Robust AI Fetching มาใช้
        - ระบบจะวนลูปเลือกโมเดลและ API Version ที่ดีที่สุดให้อัตโนมัติ (Flash 2.0/1.5) เพื่อลดโอกาสเกิด Error
        - เพิ่มระบบตรวจสอบ API Key แยกกันระหว่าง Gemini ทั่วไป และ AI Cowork
    - เตรียมความพร้อมโปรเจกต์สำหรับอัปโหลดขึ้น GitHub เพื่อไปทำงานต่อที่ทำงาน
    - แก้ไข TypeScript Errors ทั้งหมด (๒๗ รายการ) รวมถึง Unused Variables และ Type Mismatches
    - ติดตั้ง Dependencies ที่ขาดหายไป (recharts) และยืนยันการ Build ผ่าน (npm run build) เพื่อความปลอดภัยบน Vercel
    - ปรับปรุงโครงสร้างไฟล์ `ProcurementDocGenerator.ts`, `Finance.tsx` และ `AICowork.tsx` ให้สะอาดและพร้อมพัฒนาต่อ
- **๑๓ พฤษภาคม ๒๕๖๙:**
    - ติดตั้งโมดูล **AI Cowork** (Virtual Drive + Chat Hub) สมบูรณ์ 100%
    - แก้ปัญหา API Quota และระบบเชื่อมต่อ Gemini ให้มีความเสถียรสูง (Retry & Fallback)
    - เพิ่มระบบแยก API Key สำหรับงาน AI Cowork โดยเฉพาะ
- **๑๑ พฤษภาคม ๒๕๖๙ (เย็น):**
    - ติดตั้งโมดูลการเงิน/พัสดุ (Finance.tsx) และออกแบบ Database Schema ใหม่
    - วิเคราะห์ข้อมูลจากไฟล์ตัวอย่างจัดซื้อจัดจ้าง มค 69 และสรุปฟิลด์ข้อมูลที่จำเป็นลงในระบบ
- **๑๑ พฤษภาคม ๒๕๖๙:** 
    - อัปโหลดซอร์สโค้ดขึ้น GitHub สำเร็จที่: `https://github.com/officebkky-sketch/school-admin`
    - ตั้งค่าระบบ CI/CD และ Deploy บน Vercel สำเร็จที่: `https://school-admin-psi.vercel.app`
    - ติดตั้ง Git และ Vercel CLI ในเครื่องเพื่อรองรับการพัฒนาต่อเนื่อง
- **๑๑ พฤษภาคม ๒๕๖๙ (เช้า):** พัฒนาระบบรายงานเวลาเรียนเสร็จสมบูรณ์ รองรับการพิมพ์และ Excel พร้อมแก้ไขปัญหา Timezone และจำนวนนักเรียนไม่ตรงปีการศึกษา
- **๑๐ พฤษภาคม ๒๕๖๙:** จบการปรับปรุงงานสารบรรณ เริ่มเข้าสู่เฟสการจัดการข้อมูลนักเรียนข้ามปีและรายงาน LEC

---

---

## ⚠️ กฎเหล็กการพัฒนา (Strict Development Rules)
- **AiPASS-First & Executive Synthesis Mandate (กฎเหล็กสูงสุด):** ทุกการทำงานที่ต้องใช้การคิดเชิงเหตุผล (Reasoning), การคำนวณงบประมาณ/พัสดุ, การร่างสำนวนราชการ และการเขียนโค้ด **จะต้องเรียกใช้ทีมผู้เชี่ยวชาญ AiPASS ก่อนเสมอ** เพื่อประหยัด Token สูงสุด โดย Antigravity ทำหน้าที่เป็นผู้กระจายงาน (Dispatcher) และทำหน้าที่ด่านสุดท้ายคือ **ตรวจสอบความถูกต้อง (Verification/Deploy) และสังเคราะห์สรุปรายงานผลเสนอผู้บริหารเสมอ**
- **ระบบงานสารบรรณ (หนังสือรับ):** ปฏิบัติตาม Rule D (DB-First & Instant Optimistic UI) และ Rule A (Universal Dual-Runtime) เสมอ
- **การกู้คืนงาน:** หากมีการรวมโค้ดใหม่ ให้ตรวจสอบเสมอว่าระบบหนังสือรับและระบบ Multi-School ทำงานได้สมบูรณ์และแยก Supabase instance ตาม Rule C 100%

---
